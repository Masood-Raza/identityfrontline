{
  "name": "identityfrontline-assessment-tests",
  "private": true,
  "version": "1.0.0",
  "description": "Tests for the catalog-driven assessment planner. Kept out of the repo root so the Azure App Service deploy does not treat the site as a Node app.",
  "type": "module",
  "scripts": {
    "test": "node catalog.test.mjs && node wizard.test.mjs"
  },
  "devDependencies": {
    "jsdom": "^26.0.0"
  }
}
